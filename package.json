{
  "name": "yaak-plugin-openapi-local-sync",
  "private": true,
  "version": "0.1.0",
  "description": "Synchronize a Yaak workspace with a local OpenAPI file.",
  "scripts": {
    "build": "yaak plugin build",
    "dev": "yaak plugin dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@yaakapp/api": "^0.7.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
